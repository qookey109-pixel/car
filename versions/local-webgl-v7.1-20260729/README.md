# Racing Playable V7.1 - 橫式實戰 HUD 版

這版針對手機橫式 Safari 畫面再次壓縮 HUD、倒數與操作按鈕。

## 更新
- V7.1 橫式實戰 HUD
- 手機橫式高度不足時自動壓縮
- 倒數按鈕文字縮短
- FPS 標籤在手機橫式隱藏
- 底部按鈕降低高度、減少遮擋
- 開場設定面板更扁
- 保留 V6.8 強化 AI、雙強敵、音效、320 km/h、倒數起跑

直接開啟 `index.html` 可遊玩。

## 本機資料庫

V7.1 現在使用瀏覽器的 IndexedDB（資料庫名稱：`neon-racer-v7.1`）保存圈數、車數、各組合的最佳成績與每場完賽紀錄；不需要網址、帳號或伺服器。第一次啟動會自動讀取舊版 `localStorage` 資料並遷移，IndexedDB 不可用時則安全退回原本的 `localStorage`。
