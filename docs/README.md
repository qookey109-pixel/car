# Docs

目前 `docs/` 只保留主線開發需要的文件。

- `PROJECT_STATUS.md`：目前正式 Repository、版本、限制與開發原則。
- `handoffs/CURRENT_HANDOFF.md`：跨聊天室／Codex 接續時使用的最新 Handoff。

歷史恢復資料已自 `main` 清除；如需查閱，使用 `archive/pre-cleanup-20260811`，不要直接把 archive 內容覆蓋主線。
