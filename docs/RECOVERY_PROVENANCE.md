# Recovery Provenance

Repository `qookey109-pixel/car` was recreated on 2026-09-08.

A user-provided ZIP was supplied as a recovery candidate for the older V53 game. It may contain useful historical source and recovered/generated assets, but it is not treated as automatic proof of the previously developed V0.7.x Three.js experimental history.

Rules:

- Do not claim missing historical branches were recovered unless their exact source is present and verified.
- Do not overwrite newer verified repository work with older recovered candidates.
- Keep recovery candidates isolated from the active V0.8.0 modular Three.js branch until explicitly reviewed.
- Repository commits and versioned status files are the authority going forward.
- Remove macOS metadata (`__MACOSX`, `.DS_Store`) before any future recovery import.
