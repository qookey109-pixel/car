export class QualityManager{
  constructor(renderer,world,bloomPass,onChange=()=>{}){this.renderer=renderer;this.world=world;this.bloomPass=bloomPass;this.onChange=onChange;this.requested='auto';this.effective='high';this.resolution=1;this.bloom=true;this.shadows=true;this.samples=[];this.lastDecision=performance.now();this.apply({quality:'auto',resolution:1,bloom:true,shadows:true})}
  apply(s){this.requested=s.quality||this.requested;this.resolution=s.resolution??this.resolution;this.bloom=s.bloom??this.bloom;this.shadows=s.shadows??this.shadows;if(this.requested!=='auto')this.effective=this.requested;this._sync()}
  _sync(){const dpr=Math.min(window.devicePixelRatio||1,this.effective==='high'?1.6:this.effective==='medium'?1.25:1);const scale=this.resolution*(this.effective==='low'?.82:1);this.renderer.setPixelRatio(Math.max(.6,dpr*scale));this.renderer.shadowMap.enabled=this.shadows&&this.effective!=='low';if(this.world?.sun){this.world.sun.castShadow=this.renderer.shadowMap.enabled;const size=this.effective==='high'?1536:this.effective==='medium'?1024:512;this.world.sun.shadow.mapSize.set(size,size)}if(this.bloomPass){this.bloomPass.enabled=this.bloom&&this.effective!=='low';this.bloomPass.strength=this.effective==='high'?.55:.38}this.onChange(this.requested==='auto'?`AUTO · ${this.effective}`:this.effective)}
  tick(dt){if(this.requested!=='auto'||dt<=0||dt>.25)return;this.samples.push(1/dt);if(this.samples.length>180)this.samples.shift();if(this.samples.length<120)return;const now=performance.now();if(now-this.lastDecision<4000)return;const avg=this.samples.reduce((a,b)=>a+b,0)/this.samples.length;let next=this.effective;if(avg<39){if(next==='high')next='medium';else if(next==='medium')next='low'}else if(avg>56&&now-this.lastDecision>8000){if(next==='low')next='medium';else if(next==='medium')next='high'}if(next!==this.effective){this.effective=next;this._sync();this.samples.length=0}this.lastDecision=now}
}
